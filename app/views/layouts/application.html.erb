<!DOCTYPE html>
<html>
  <head>
    <title>VetorEditora</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap" rel="stylesheet">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav id="navbar">
      <%= link_to root_path do %>
        <%= image_tag 'logo-vetor-presente.png' %>
      <% end %>
      
      <% if psychologist_signed_in? %>
        <div class="menu-nav">
          <p><%= current_psychologist.email %></p>
          <%= button_to t(:log_out), destroy_psychologist_session_path, method: :delete, class: 'button-two' %>
        </div>
      <% else %>
        <% if current_page?(root_path) %>
          <%= link_to 'Fazer login', new_psychologist_session_path, class: 'button' %>
        <% end %>
      <%end%>
    </nav>

    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <%= yield %>
  </body>
</html>
